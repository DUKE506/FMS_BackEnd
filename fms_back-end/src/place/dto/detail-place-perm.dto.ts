



export class PlacePerm {
    machinePerm: boolean;

    // 전기 권한
    electricPerm: boolean;

    // 승강 권한
    liftPerm: boolean;

    // 소방 권한
    firePerm: boolean;

    // 건축 권한
    constructPerm: boolean;

    // 네트워크 권한
    networkPerm: boolean;

    // 미화 권한    
    beautyPerm: boolean;

    // 미화 권한
    securityPerm: boolean;

    // 에너지 권한    
    energyPerm: boolean;

    // 민원 권한
    vocPerm: boolean;
}